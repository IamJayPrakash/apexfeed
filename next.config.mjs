/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ["images.unsplash.com", "picsum.photos", "localhost", "localhost:3000", "lh3.googleusercontent.com"],
  },
};

export default nextConfig;
